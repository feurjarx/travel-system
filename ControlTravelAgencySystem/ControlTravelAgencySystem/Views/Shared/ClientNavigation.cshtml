<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">XX World <i class="fa fa-globe"></i></a>
        </div>

        <ul class="collapse nav navbar-nav navbar-collapse">
            <li class="active">
                <a href="#custom-tour-tab" data-toggle="tab">Главная</a>
            </li>
            <li>
                <a href="#completed-tour-tab" data-toggle="tab">Готовые курорты</a>
            </li>
            @if (ViewBag.Session)
            {
                <li class="visible-xs">
                    <a href="/admin" class="">Панель управления
                        <i class="fa fa-cog"></i>
                    </a>
                </li>
                <li class="visible-xs">
                    <a href="/admin/logout" class="">Выйти
                        <i class="fa fa-sign-out"></i>
                    </a>
                </li>
            }
        </ul>

        <ul class="nav navbar-nav">
            @if (@Request.Url.LocalPath.ToString() == @"/" || @Request.Url.LocalPath.ToString() == @"/Home/Index")
            {
                <li class="visible-xs">
                    <button id="callout-begin-signup-btn" type="button" class="hvr-icon-push btn btn-primary btn-lg btn-block" onclick="location.href='@Url.Action("FavoriteList", "Rooms")'">Список пожеланий</button>
                </li>
            }
            
            @if (@Request.Url.LocalPath.ToString() == @"/Rooms/FavoriteList")
            {
                <li class="visible-xs">
                    <button id="callout-end-signup-btn" class="hvr-icon-fade btn btn-primary btn-lg btn-block" data-toggle="modal" data-target="#callout-modal">Регистрация</button>
                </li>
            }
            
        </ul>

        <ul class="nav navbar-nav pull-right hidden-xs">


            @if (@Request.Url.LocalPath.ToString() == @"/" || @Request.Url.LocalPath.ToString() == @"/Home/Index")
            {
                <li>
                    <button id="callout-begin-signup-btn" type="button" class="hvr-icon-push btn btn-primary btn-lg" onclick="location.href='@Url.Action("FavoriteList", "Rooms")'">Список пожеланий</button>
                </li>
            }

            @if (@Request.Url.LocalPath.ToString() == @"/Rooms/FavoriteList")
            {
                <li>
                    <button id="callout-end-signup-btn" class="hvr-icon-fade btn btn-primary btn-lg" data-toggle="modal" data-target="#callout-modal">Регистрация</button>
                </li>
            }

            @if (ViewBag.Session)
            {
                <li>
                    <a href="/admin" class="">
                        Панель управления
                        <i class="fa fa-cog"></i>
                    </a>
                </li>
                <li>
                    <a href="/admin/logout" class="">
                        Выйти
                        <i class="fa fa-sign-out"></i>
                    </a>
                </li>
            }

        </ul>
    </div>
</nav>